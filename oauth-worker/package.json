{
  "name": "polastri-cms-oauth",
  "version": "1.0.0",
  "private": true,
  "description": "OAuth proxy (Cloudflare Worker) para el login de Decap CMS en /admin",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
